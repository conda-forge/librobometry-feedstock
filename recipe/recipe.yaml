# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

context:
  version: "1.2.8"

recipe:
  name: robometry-split
  version: ${{ version }}

source:
  - url: https://github.com/robotology/robometry/archive/refs/tags/v${{ version }}.tar.gz
    sha256: fda43c638cb06c7f14d77700477ca1314a59aae673ef29e42dbe43b159b0e4b1

build:
  number: 0

outputs:
  - package:
      name: librobometry
    build:
      script:
        - if: unix
          then: build_cxx.sh
        - if: win
          then: bld_cxx.bat
    requirements:
      build:
        - ${{ compiler('c') }}
        - ${{ stdlib('c') }}
        - ${{ compiler('cxx') }}
        - cmake
        - pkg-config
        - ninja
      host:
        - ycm-cmake-modules
        - libmatio-cpp
        - libboost-devel
        - nlohmann_json
        - libyarp
      run:
        - libboost-headers
      run_exports:
        - ${{ pin_subpackage('librobometry', upper_bound='x.x.x') }}

    tests:
      - package_contents:
          include:
            - robometry/BufferManager.h
          lib:
            - robometry
      - script:
          - cmake-package-check robometry
        requirements:
          run:
            - ${{ compiler('c') }}
            - ${{ compiler('cxx') }}
            - cmake-package-check

  - package:
      name: robometry
    requirements:
      run:
        - ${{ pin_subpackage('librobometry', exact=True) }}
      run_exports:
        - ${{ pin_subpackage('librobometry', upper_bound='x.x.x') }}
    tests:
      - script:
          - cmake-package-check robometry
        requirements:
          run:
            - ${{ compiler('c') }}
            - ${{ compiler('cxx') }}
            - cmake-package-check

about:
  homepage: https://github.com/robotology/robometry
  license: BSD-3-Clause
  license_file: LICENSE
  summary: Telemetry library for logging data.

extra:
  recipe-maintainers:
    - traversaro
